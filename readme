Программа работает по следующему принцыпу:
Суть в том, что берется некая картинка и выделяется область с которой нужно получить текст.
1. Берется определенная фотография;
2. Запускаешься 'Print(x, y)_on_window';
    Она отслеживает курсор мыши в реальном времени и выводит на экрам координаты X, Y
3. Полученные координаты мы вписываем в подпрограмму 'coordinates' в цикле While
   в строку 'screen = np.array(ImageGrab.grab(bbox=(x1,y1 ,x2,y2)))';
4. Открываем фотографию в Python, и запускаем программу 'text_to_photo', после сробавываения
   в консоли появиться текст с фотографии этой области: (x1,y1 ,x2,y2).

Подпрограмма 'coordinates' берет целую фотографию переданную в Python, выделает обслать: (x1,y1 ,x2,y2) делает скриншот
и перезапысывает в папку проекта этот скриншот за место начальной фотографии и выводит текст с этого скриншота.